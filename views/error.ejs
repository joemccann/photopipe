<!DOCTYPE html>
<html>
<%- include head %>
<body>
   <div id="main">
     
     <%- include nav %>     
     
     <section id="errorSection">
      <% if(type === 'instagram') { %>
      <p>Something went wrong with the Instagram connection.</p>
      <p class="error_redirect">
        <a href="/<%= type %>">Try again here.</a>
      </p>
      <% }else if(type === 'facebook') { %>
      <p>Something went wrong with the Facebook connection.</p>
      <p class="error_redirect">
        <a href="/<%= type %>">Try again here.</a>
      </p>
      <% }else if(type === 'twitter') { %>
      <p><%= error.error_description || "Something went wrong with the twitter connection." %></p>
      <p class="error_redirect">
        <a href="/<%= type %>">Try again here.</a>
      </p>
      <% }else{ %>
      <p>Something simply went wrong.</p>
      <p class="error_redirect">
        <a href="/">Head to PhotoPipe Home Page</a>
      </p>
      <% } %>
     </section>
     
     
   </div>
   
 </body>
</html>